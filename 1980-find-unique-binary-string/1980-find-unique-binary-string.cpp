class Solution {
public:
    string findDifferentBinaryString(vector<string>& nums) {
        
        int n = nums.size();
        
        for(int i = 0; i < n; ++i)
        {
            if(nums[0][i] = nums[i][i] == '0' ? '1' : '0');
        }
        
        return nums[0];
    }
};